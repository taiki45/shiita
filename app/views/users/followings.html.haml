- content_for :sidebar do
  = render partial: "sidebar", locals: {user: @user}

.page-header
  %h1
    %span= login? && @user == current_user ? "Your followings" : "Followings"
    %small= @user.email

= render partial: "share/user_table", locals: {users: @user.followings}
