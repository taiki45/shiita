- if params[:controller] == "items"
  %h1= item.title
- else
  %h1= link_to item.title, item

%p
  %strong tags:
  - item.tags.each do |tag|
    - #TODO Add link to the tag
    %span.badge
      %i.icon-tag
        = tag

.well
  :markdown
    #{item.source}
